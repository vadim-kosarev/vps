# vkosarev.name — iptables проброс портов frps-туннелей

## Контекст

`frps.toml` настроен с `proxyBindAddr = "127.0.0.1"` — все frp-туннели слушают только
на localhost. Чтобы сделать конкретный порт публичным, используется iptables DNAT.

## Предварительные условия (выполнены один раз)

```bash
sysctl -w net.ipv4.conf.all.route_localnet=1
echo "net.ipv4.conf.all.route_localnet=1" >> /etc/sysctl.conf
# iptables-persistent уже установлен
```

## Команды для добавления нового порта

```bash
iptables -t nat -A PREROUTING -p tcp --dport <PORT> -j DNAT --to-destination 127.0.0.1:<PORT>
iptables -A FORWARD -p tcp -d 127.0.0.1 --dport <PORT> -j ACCEPT
netfilter-persistent save
```

## Текущие проброшенные порты

| Порт  | Туннель (frps)  | Сервис         | Дата добавления |
|-------|----------------|----------------|-----------------|
| 41404 | luigi-sync     | Resilio Sync   | 2026-06-12      |
