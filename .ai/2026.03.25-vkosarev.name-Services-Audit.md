# 2026-03-25 — Аудит сервисов vkosarev.name (что не попало на индексную страницу)

Данные собраны командами `docker ps`, `ss -tlnp`, frps API (`/api/proxy/tcp`),
запросами к портам и чтением БД 3x-ui.

---

## Уже на индексной странице (`nginx/www/vkosarev.name/index.html`)

| Порт | Сервис | Контейнер |
|------|--------|-----------|
| 80 | HTTP → HTTPS редирект | nginx |
| 1443 | Статический сайт | nginx |
| 981 | HIKCAM (HikVision camera UI) 🔐 | nginx → frps → cam1 |
| 2443 | MTProxy (Telegram) | mtproxy |
| 2055 | 3x-ui панель 🔐 | 3x-ui |
| 3000 | Grafana 🔐 | grafana |
| 3200 | Mermaid Live Editor | mermaid |
| 4041 | Subsonic (luigi) | nginx → frps → luigi |
| 5001 | Frigate NVR 🔐 | nginx → frps → frigate |
| 5201 | iperf3 | iperf3 |
| 7599 | frps Dashboard 🔐 | frps |
| 7601 | Immich | nginx → frps → immich |
| 9090 | Prometheus | prometheus |
| 9443 | Portainer 🔐 | portainer |

---

## НЕ на индексной странице

### 1. `xui-exporter` — Prometheus exporter для 3x-ui

| Параметр | Значение |
|----------|----------|
| **Порт** | `3001` |
| **Image** | `m4l3vich/3x-ui-prometheus-exporter:latest` |
| **Endpoint** | `http://vkosarev.name:3001/metrics` → HTTP 200 |
| **Назначение** | Prometheus экспортер метрик 3x-ui (трафик, пользователи, inbound-статистика) |
| **Публичный?** | Да (0.0.0.0:3001) — но нет смысла показывать на дашборде |

**Решение:**  
- [ ] Закрыть от публичного доступа (добавить в nginx proxy с auth, или ограничить firewall)  
- [ ] Добавить в Prometheus scrape config (если не добавлен)

---

### 2. `node_exporter` — системный Prometheus exporter

| Параметр | Значение |
|----------|----------|
| **Порт** | `9100` |
| **Процесс** | `node_exporter` (системный, не Docker) |
| **Endpoint** | `http://vkosarev.name:9100/metrics` → HTTP 200 |
| **Назначение** | Метрики хоста: CPU, RAM, диск, сеть |
| **Публичный?** | Да (`*:9100`) — **⚠️ НЕ ДОЛЖЕН быть публичным!** |

**Решение:**  
- [ ] Добавить правило firewall: запретить внешний доступ к 9100  
- [ ] Или добавить в nginx как proxy с basic auth  
- [ ] Проверить что Prometheus его скрейпит

---

### 3. 3x-ui xray inbounds — порты VPN/proxy-протоколов

Это не веб-сервисы, добавлять на дашборд не нужно, но зафиксировать полезно.

| Порт | Протокол | Remark | Публичный? |
|------|----------|--------|------------|
| `443` | ??? | (не в БД inbounds, но docker-proxy слушает) | Да |
| `8080` | VLESS | "RU-PL" | Да |
| `8443` | Mixed | "mixed -msk MTProxy" | Да |
| `10126` | HTTP proxy | "http-proxy-10126" | Да |
| `34819` | VLESS | "EU.AMAZON" | Да |

> **Примечание по порту 443:** в БД `inbounds` не зафиксирован, но docker-proxy слушает.  
> Возможно — subscription/redirect endpoint самого 3x-ui, или пустой inbound.

**Решение:**  
- [ ] Разобраться что на 443 (3x-ui subscription URL?)  
- [ ] Проверить не конфликтует ли 8443 с MTProxy (оба обрабатывают MTProxy трафик?)

---

### 4. `portainer` Edge Agent порт 8000

| Параметр | Значение |
|----------|----------|
| **Порт** | `8000` |
| **Назначение** | Edge Agent tunnel / API (не веб-UI) |
| **Публичный?** | Да (0.0.0.0:8000) |

> Portainer UI — на 9443 (уже есть на дашборде). Порт 8000 — для Portainer Edge Agents.

**Решение:**  
- [ ] Если Edge Agents не используются — закрыть 8000 от публики

---

### 5. `luigi-grafana` — Grafana на удалённой машине "luigi"

| Параметр | Значение |
|----------|----------|
| **frps туннель** | `luigi-grafana` |
| **localhost порт** | `127.0.0.1:3003` |
| **HTTP статус** | `302 → /login` ✅ (сервис живой!) |
| **Назначение** | Grafana, запущенная на машине "luigi" (home server?) |
| **Через nginx?** | ❌ Нет — нет nginx proxy на 3003 |
| **Публичный?** | Нет (только localhost) |

**Решение:**  
- [ ] Добавить nginx server-блок `3031 ssl` → `proxy_pass http://localhost:3003`?  
- [ ] Добавить на индексную страницу как "Luigi: Grafana :3031"?

---

### 6. `luigi-torrent` — торрент-клиент на "luigi"

| Параметр | Значение |
|----------|----------|
| **frps туннель** | `luigi-torrent` |
| **localhost порт** | `127.0.0.1:24222` |
| **HTTP статус** | нет ответа (curl timeout) |
| **Назначение** | Веб-интерфейс торрент-клиента на "luigi" |
| **Через nginx?** | ❌ Нет |

> **⚠️ Внимание:** nginx проксирует `3181 → localhost:3180`, но `3180` нигде не слушает!  
> Вероятно, старый конфиг — torrent переехал на порт `24222`.  
> Либо nginx нужно исправить на `3181 → localhost:24222`.

**Решение:**  
- [ ] Исправить nginx: изменить `proxy_pass http://localhost:3180` на `http://localhost:24222`  
- [ ] Проверить что сервис там живёт (curl timeout — возможно, не HTTP?)  
- [ ] Добавить на дашборд как "Luigi: Torrent :3181"?

---

### 7. `starlight-vnc` — VNC на машине "starlight"

| Параметр | Значение |
|----------|----------|
| **frps туннель** | `starlight-vnc` |
| **localhost порт** | `127.0.0.1:5900` |
| **Протокол** | VNC (не HTTP!) |
| **Через nginx?** | ❌ Нет — nginx не умеет проксировать VNC |
| **Публичный?** | Нет (только localhost через frps) |

**Решение:**  
- [ ] Для веб-доступа к VNC — рассмотреть `noVNC` (веб-клиент)  
- [ ] Или использовать VNC-клиент напрямую через SSH-туннель/frp-туннель  
- [ ] Добавить на дашборд как "Starlight: VNC (noVNC)" если настроить

---

### 8. `cam1-ui-https` — HTTPS UI камеры HikVision

| Параметр | Значение |
|----------|----------|
| **frps туннель** | `cam1-ui-https` |
| **localhost порт** | `127.0.0.1:982` |
| **Через nginx?** | ❌ Нет — нет nginx proxy на 982 |
| **Примечание** | HTTP UI (`cam1-ui-http` → 980) уже есть через nginx:981 |

**Решение:**  
- [ ] HTTPS версия камеры нужна? Или достаточно порта 981 (HTTP proxy)?  
- [ ] Если нужна — добавить nginx блок с `proxy_pass https://localhost:982`

---

### 9. RTSP потоки (не HTTP)

| frps туннель | localhost порт | Описание |
|-------------|---------------|----------|
| `cam1-high` | `127.0.0.1:554` | RTSP камеры (высокое качество) |
| `frigate-rtsp` | `127.0.0.1:9554` | RTSP Frigate |

> Это RTSP потоки — nginx их не проксирует (нужен `nginx-rtmp` или другой подход).  
> Доступны только из локальной сети через frp-туннели (для RTSP-клиентов).

**Решение:**  
- [ ] Задокументировать URL RTSP: `rtsp://vkosarev.name:554/` (если frps пробросит TCP)  
- [ ] Добавить на индексную страницу info-только (без clickable link)

---

## Сводная таблица — решения по каждому сервису

| Сервис | Порт | Рекомендация |
|--------|------|-------------|
| xui-exporter | 3001 | Закрыть от публики, добавить в Prometheus |
| node_exporter | 9100 | **Закрыть firewall!** (сейчас открыт) |
| 3x-ui port 443 | 443 | Разобраться что там |
| portainer edge | 8000 | Закрыть если Edge Agents не нужны |
| luigi-grafana | 3003 | Добавить nginx proxy + на дашборд |
| luigi-torrent | 24222 | Исправить nginx (3181→3180 → 3181→24222) + дашборд |
| starlight-vnc | 5900 | noVNC или только локальный доступ |
| cam1-ui-https | 982 | Оставить без nginx (981 HTTP достаточно?) |
| cam1-rtsp | 554 | Только RTSP, задокументировать |
| frigate-rtsp | 9554 | Только RTSP, задокументировать |

---

## Приоритетные действия

1. **Срочно:** Закрыть `9100` (node_exporter) от внешнего доступа — сейчас `*:9100`
2. **Важно:** Исправить nginx `3181 → 24222` для luigi-torrent  
3. **Полезно:** Добавить nginx proxy для `luigi-grafana` (3003) и на дашборд  
4. **Опционально:** xui-exporter в Prometheus scrape, portainer edge port

