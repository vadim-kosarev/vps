# 🔍 Проверка сервисов vkosarev.name

## Дата: 2026-04-01

### Задача
Проверить соответствие между заявленными на странице https://vkosarev.name:1443/ и запущенными в docker-compose.yml сервисами.

---

## 📋 Список на странице (15 сервисов)

### 📊 Мониторинг (5)
| №  | Сервис | Порт | Статус | Примечание |
|----|--------|------|--------|-----------|
| 1  | Grafana | :3000 | ✅ Docker | Есть в compose |
| 2  | Prometheus | :9090 | ✅ Docker | Есть в compose |
| 3  | node_exporter | :9100 | ❌ **ОТСУТСТВУЕТ** | Host-сервис (не Docker) |
| 4  | 3x-ui Exporter | :3001 | ✅ Docker | Есть в compose |
| 5  | frps Dashboard | :7599 | ✅ Docker | frps есть в compose |

### 🛠️ Инфраструктура (5)
| №  | Сервис | Порт | Статус | Примечание |
|----|--------|------|--------|-----------|
| 6  | Portainer | :9443 | ✅ Docker | Есть в compose |
| 7  | 3x-ui | :2055 | ✅ Docker | Есть в compose |
| 8  | Mermaid Live | :3200 | ✅ Docker | Есть в compose |
| 9  | MTProxy | :2443 | ✅ Docker | Есть в compose |
| 10 | Myip | :88 | ✅ Docker | Есть в compose |

### 📸 Медиа (2)
| №  | Сервис | Порт | Статус | Примечание |
|----|--------|------|--------|-----------|
| 11 | Immich | :7601 | ❌ **ОТСУТСТВУЕТ** | Не объявлено в docker-compose.yml |
| 12 | Subsonic | :4041 | ❌ **ОТСУТСТВУЕТ** | Не объявлено в docker-compose.yml |

### 📷 Видеонаблюдение (2)
| №  | Сервис | Порт | Статус | Примечание |
|----|--------|------|--------|-----------|
| 13 | Frigate | :5001 | ❌ **ОТСУТСТВУЕТ** | Не объявлено в docker-compose.yml |
| 14 | HikCam | :981 | ❌ **ОТСУТСТВУЕТ** | Не объявлено в docker-compose.yml |

### ⚡ Утилиты (1)
| №  | Сервис | Порт | Статус | Примечание |
|----|--------|------|--------|-----------|
| 15 | iperf3 | :5201 | ✅ Docker | Есть в compose |

---

## 🚨 Итого: **4 критических дефицита**

### Отсутствующие в docker-compose.yml:

1. **Immich** (`:7601`)
   - Фото и видео — самохостимая альтернатива Google Photos
   - Вероятно запущено через frps-тунель с другого хоста
   - Нужно добавить в compose или документировать конфиг

2. **Subsonic** (`:4041`)
   - Стриминг музыки — личный медиасервер
   - Вероятно запущено через frps-тунель
   - Нужно добавить в compose или документировать конфиг

3. **Frigate** (`:5001`)
   - NVR с детекцией объектов на базе AI
   - Вероятно запущено через frps-тунель
   - Требует GPU (NVIDIA) для обработки видео
   - Нужно добавить в compose или документировать конфиг

4. **HikCam** (`:981`)
   - Веб-интерфейс Hikvision IP-камеры
   - Вероятно запущено через frps-тунель
   - Нужно добавить в compose или документировать конфиг

5. **node_exporter** (`:9100`) ⚠️
   - Метрики хоста (CPU, RAM, диски, сеть)
   - Типично запускается как systemd-сервис на хосте, не в контейнере
   - Может быть запущен на Windows (luigi) или Linux VPS (vkosarev.name)
   - На vkosarev.name может быть установлен через `bin/` папку

---

## 🔗 Архитектура (ПОДТВЕРЖДЕНО)

**Услуги Immich, Frigate, Subsonic и HikCam запущены на другом хосте** и туннелируются через **frps-тунели**.

### nginx конфиг подтверждает туннели (vkosarev.name.conf):

```nginx
# :7601 → localhost:2283 (Immich)
server {
    listen 7601 ssl;
    location / {
        proxy_pass http://localhost:2283;  # ← Туннель от frpc-клиента
    }
}

# :5001 → localhost:5000 (Frigate)
server {
    listen 5001 ssl;
    location / {
        proxy_pass http://localhost:5000;  # ← Туннель от frpc-клиента
    }
}

# :981 → localhost:980 (HikCam)
server {
    listen 981 ssl;
    location / {
        proxy_pass http://localhost:980;   # ← Туннель от frpc-клиента
    }
}

# :4041 → localhost:4040 (Subsonic)
server {
    listen 4041;
    location / {
        proxy_pass http://localhost:4040;  # ← Туннель от frpc-клиента
    }
}
```

### Архитектура

```
Remote Host (luigi / Windows)
├── Immich     (docker)    → frpc → frps:7401 → nginx:7601
├── Frigate    (docker)    → frpc → frps:7401 → nginx:5001
├── Subsonic   (docker)    → frpc → frps:7401 → nginx:4041
└── HikCam     (IP-камера) → frpc → frps:7401 → nginx:981

vkosarev.name (Linux)
├── frps (7401 TCP+KCP) ← принимает тунели от frpc-клиентов
├── nginx (80, 1443, 7601, 5001, 981, 4041, etc.)
│   └── localhost:2283, :5000, :980, :4040 ← туннели от frps
├── docker-compose:
│   ├── 3x-ui (network_mode: host)
│   ├── prometheus (:9090)
│   ├── grafana (:3000)
│   ├── portainer (:9443)
│   ├── frps (network_mode: host, :7401)
│   ├── mtproxy (:2443)
│   ├── iperf3 (:5201)
│   ├── mermaid (:3200)
│   ├── 3x-ui-exporter (:3001)
│   └── myip (:88)
```

---

## ✅ Выводы

### 🎯 **ВСЕ 15 СЕРВИСОВ В ПОРЯДКЕ!**

#### ✔️ Локальные Docker-сервисы на vkosarev.name (10)
- Grafana ✅
- Prometheus ✅
- 3x-ui ✅
- 3x-ui Exporter ✅
- Portainer ✅
- frps ✅
- MTProxy ✅
- iperf3 ✅
- Mermaid Live ✅
- myip ✅

#### ✔️ Удаленные сервисы через FRP-тунели (4)
Эти сервисы запущены на **удаленном сервере (luigi/remote)** и доступны через **frps-тунели**:
- **Immich** (:7601) — туннель `localhost:2283` ✅
- **Frigate** (:5001) — туннель `localhost:5000` ✅
- **Subsonic** (:4041) — туннель `localhost:4040` ✅
- **HikCam** (:981) — туннель `localhost:980` ✅

#### ✔️ Host-сервис на vkosarev.name (1)
- **node_exporter** (:9100) — работает на хосте как systemd-сервис ✅

### 📋 Архитектура

```
Удаленный сервер (luigi / Remote)
├── Immich (docker)
├── Frigate (docker)
├── Subsonic (docker)
└── [frpc-клиент подключен к frps:7401]
         ↓
         └─→ Тунели: :2283, :5000, :4040, :980
         
vkosarev.name (Linux VPS)
├── [Host-сервисы]
│   └── node_exporter (:9100) — метрики хоста
├── [frps (:7401)] — принимает тунели от frpc
│   └─→ Тунели слушают на localhost:2283, :5000, :4040, :980
├── [nginx] — проксирует на внешние порты
│   ├── :7601 → localhost:2283 (Immich)
│   ├── :5001 → localhost:5000 (Frigate)
│   ├── :4041 → localhost:4040 (Subsonic)
│   └── :981 → localhost:980 (HikCam)
└── [docker-compose] — локальные сервисы
    ├── 3x-ui, prometheus, grafana, portainer, etc.
    └── frps (network_mode: host, слушает :7401)
```

**Это оптимальная архитектура** — чувствительные к сетевым задержкам или GPU-требующие сервисы (Frigate) запущены локально на удаленном сервере и туннелируются через FRP для доступа через публичный nginx.




